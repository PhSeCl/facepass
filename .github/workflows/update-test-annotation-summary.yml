name: Update Test Annotation Summary

on:
  workflow_dispatch:
  schedule:
    - cron: "0 3 * * *"
  push:
    branches:
      - main
    paths:
      - "dataset/test/annotation.json"
      - "dataset/test/images/**"
      - "scripts/summarize_test_annotations.py"
      - "scripts/json2jsonl.py"
      - ".github/workflows/update-test-annotation-summary.yml"

permissions:
  contents: write

jobs:
  refresh-readme-summary:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install converter dependencies
        run: pip install pillow

      - name: Sync regulation annotations.jsonl from annotation.json
        run: python scripts/json2jsonl.py

      - name: Refresh README summary block
        run: python scripts/summarize_test_annotations.py --write-readme

      - name: Commit synced files when changed
        shell: bash
        run: |
          if git diff --quiet -- README.md dataset/test/annotations.jsonl; then
            echo "README summary and annotations.jsonl unchanged"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md dataset/test/annotations.jsonl
          git commit -m "data: sync annotations.jsonl and test annotation summary"
          git push
